class Park 
    attr_reader :name :address :phone, :serivces
    
    def initialze(name, address, :phone)
        @name = name
        @address = address
        @phone = phone
        @serivces = serivces
    end

    def add_service(Park)
        @serivces << serivce
    end

    def admission_price(vehicle)
    when @park_services
        vehicle.charge(50)
        end
    end
    
    current_vehicles = {
        vehicle_make: "Honda"
        vehicle_model: "Civic"
}

    end
end

#needs more functionality 
#needs more statments
#totally stumped atm 